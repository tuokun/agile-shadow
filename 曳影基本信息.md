# 曳影 (Yeying)

基于 Rime 输入法框架，从零开发的个人 Android 输入法。参考语燕输入法 (yuyansdk) 的实现思路，全部 Kotlin 代码自行编写。

## 项目名

- 中文名：曳影
- 英文标识：Yeying
- 包名：`dev.yeying.ime`

## 架构

```
┌─────────────────────────────────────┐
│  Compose UI 层（全新）               │  键盘、候选栏、符号面板、设置
├─────────────────────────────────────┤
│  桥接层（新建）                      │  InputMethodService ↔ Compose
├─────────────────────────────────────┤
│  引擎层（自行编写）                  │  Rime JNI 封装、T9 映射、数据结构
├─────────────────────────────────────┤
│  数据层                             │  rime-ice 默认词库、用户词库、偏好、剪贴板
└─────────────────────────────────────┘
```

## 功能范围

### 保留
- 九键拼音 (T9)、全键拼音 (QWERTY)、英文输入
- 手写输入
- 符号/Emoji 面板、剪贴板管理
- 主题系统、键盘布局模式（单手/悬浮/全面屏）

### 移除
- 双拼（7 种方案）、五笔画输入
- 花漾字、常用语/前缀输入、语音输入

## UI

- **Liquid Glass 毛玻璃效果**（全局：键盘背景、候选栏、符号面板等）
- 实现方式：模拟玻璃（半透明渐变 + 高光 + 内发光 + 边框），非实时模糊
- 要求：高 alpha 0.85~0.95，看不清下方应用内容
- 参考：`D:/Dev/project/GitHub-Store` 的 BottomNavigation 毛玻璃实现

## 词库

- 内置默认：**rime-ice 雾凇拼音**
- 用户可自行替换为任意 Rime 兼容方案/词库
- 双目录架构：shared_data_dir（只读内置）+ user_data_dir（可写，优先级更高）

## Emoji

- 系统原生（Unicode 文本 + EmojiCompat 兼容），不引入图片资源



## 开源引用

本项目使用了语燕输入法 (yuyansdk) 的开源部分：
- Rime JNI 桥接层（`com.yuyan.inputmethod.core.Rime`）
- 手写识别模块的原生库封装（`com.yuyan.inputmethod.core.HandWriting`）
- 相关 Rime 数据结构定义

以上代码因 JNI 二进制接口（静态注册）限制而采纳，详见项目 `NOTICE` 文件。

## 原生库

| 库 | 用途 |
|---|---|
| libyuyanime.so | Rime 引擎 JNI |
| libhandwriting.so | 手写 JNI 封装 |
| libhwInterface.so | 手写中间层（搜狗） |
| libgpen_handwriter.so | GPU 笔迹加速 |
| libSogouShell.so | 手写运行时依赖（搜狗核心） |

## 关键参考

| 项目 | 路径 | 用途 |
|------|------|------|
| yuyansdk (语燕) | `D:/Dev/project/ime/yuyansdk/` | 实现思路参考 |
| rime-ice (雾凇拼音) | `D:/Dev/project/ime/rime-ice/` | 默认词库来源 |
| GitHub-Store | `D:/Dev/project/GitHub-Store` | Liquid Glass 效果参考 |
